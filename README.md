# Paperlogic Landing

The marketing and landing page for [Paperlogic](https://paperlogic.lk) — enterprise document digitization and AI extraction platform.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env.local` and fill in your values
3. Run the app:
   `npm run dev`
